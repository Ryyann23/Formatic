import 'package:flutter/widgets.dart';

/// Wraps the widget tree with global providers/injectors.
Widget buildAppProviders({required Widget child}) {
  // Add MultiProvider or other injection layers here when needed.
  return child;
}
